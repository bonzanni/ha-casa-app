# claude-agent-sdk pin. History: 0.1.72 -> 0.2.87 (v0.38.0, stderr-callback
# exception isolation); 0.2.87 -> 0.2.114 (v0.59.1, Dependabot #104);
# 0.2.114 -> 0.2.128 (v0.131.0, supersedes Dependabot #268). Per the upstream
# release notes (github.com/anthropics/claude-agent-sdk-python/releases):
# 0.2.121 passes --resume/--session-id as single =-joined argv tokens
# (flag-injection hardening); 0.2.127 keeps stdin open while background tasks
# are in flight (fixes SDK-MCP "Stream closed" failures that also silently
# bypassed PreToolUse hooks); 0.2.126 adds ResultMessage.terminal_reason +
# typed model_usage (additive).
# NOTE (v0.131.0): Casa does NOT run the SDK-bundled CLI — every production
# ClaudeAgentOptions passes cli_path=/usr/local/bin/claude
# (claude_runtime.CLAUDE_CLI_PATH), installed by the Dockerfile npm pin and
# boot-verified against claude_runtime.CLAUDE_CLI_VERSION. Keep the pip pin,
# the npm pin, and CLAUDE_CLI_VERSION in lockstep;
# tests/test_cli_sdk_pin_assert.py enforces the npm pins (both Dockerfiles)
# against CLAUDE_CLI_VERSION, and this pip pin against the test's own
# SDK_VERSION constant — bump that constant with this line.
# G-1 ('NoneType has no attribute items' from hook_0..3 on Edit) is mitigated
# Casa-side by H-2 (v0.36.1): every hook callback returns {} / _deny(...),
# never None (hooks.py) — Casa is immune regardless of the upstream guard.
claude-agent-sdk==0.2.128
python-telegram-bot==22.7
# Bumped 3.13.5 -> 3.14.1 (v0.59.1): clears 11 security advisories (deserialization,
# HTTP request-smuggling/pipelining, websocket memory-limit bypass, etc.), all
# fixed by <=3.14.1. Dependabot #97 (branch deleted before recreate).
# Bumped 3.14.1 -> 3.14.3 (v0.221.0): clears Dependabot #14 (GHSA-cq5v-8q36-5273,
# HIGH — out-of-bounds heap read in the C HTTP response parser's error path on a
# malformed chunked response, fixed 3.14.3), #13 (GHSA-mfx4-hv73-q22v, request
# smuggling via WebSocket upgrade) and #12 (GHSA-mq44-7p77-q5h7, WS client
# accepted compressed frames without negotiated permessage-deflate), both 3.14.2.
aiohttp==3.14.3
apscheduler==3.11.2
pyyaml==6.0.3
jsonschema==4.23.0
opentelemetry-api>=1.43.0
# Declared in v0.222.0: both were imported DIRECTLY (anyio by
# casa_engagement_channel.py, pydantic by ha_mcp_facade.py) while only
# transitive pins from mcp/claude-agent-sdk guaranteed them — a loan, not a
# declaration; tests/test_declared_dependencies.py now fails on any such
# undeclared import. Compatible ranges rather than exact pins, because both
# are shared with mcp and the SDK and an exact pin here would fight their
# resolvers on every bump.
anyio>=4,<5
pydantic>=2,<3
# Declared for #665: channels/telegram.py imports httpx DIRECTLY (the
# pre-send-cause classifier reads httpx.ConnectError/ConnectTimeout/
# PoolTimeout off TelegramError.__cause__) while only python-telegram-bot's
# transitive pin guaranteed it. Compatible range, not an exact pin, for the
# same reason as anyio/pydantic above: PTB owns the working version and an
# exact pin here would fight its resolver on every bump.
httpx>=0.27,<1
# E-12 (v0.37.0): Channels MCP capability — used by casa_engagement_channel.
# Build-time probe (docs/superpowers/specs/2026-05-12-e12-claude_code-channels-A6-findings.md
# §A.6.1) confirmed FastMCP `instructions=` kwarg + `Server.create_initialization_options(
# experimental_capabilities={...})` bootstrap path on this version range.
mcp>=1.28.1,<2
# Personality Phase A (Task 2): RFC 8785 (JSON Canonicalization Scheme)
# canonical bytes for the immutable speaker-provenance codec — the
# `casa-source-v1.` reserved Hindsight tag must encode a byte-stable
# canonical JSON payload so encode/decode round trips are deterministic.
rfc8785==0.1.4
# Personality Phase A (Task 3): ACCEPTED-CommonMark validation for the
# strict persona-pack loader's persona.md parsing (markdown_sections.py) —
# rejects unsupported Markdown tokens/raw HTML and does byte-preserving
# source-slice section extraction.
markdown-it-py==4.0.0
